{% extends 'admin-datatable.html.twig' %}

{% block title %}
Remplaclinic :: Admin :: Clinique
{% endblock %}

{% block page_title %}
Clinique
{% endblock %}


{% block body %}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <div class="row mb-3">
                    <div class="col-12">
                        <a class="btn btn-secondary btn-flat" id="btn-clinic-new" href="{{ path('app_admin_clinic_new') }}">
                            <i class="fas fa-plus"></i> Ajouter un clinique
                        </a>
                    </div>
                </div>
                <div class="row">
                    <div class="col-12">
                        <table id="tbl-clinics" class="table table-hover"
                            data-url="{{ path('api_clinic_get') }}"
                            data-delete-url="{{ path('api_user_delete', {id: '0000000000'}) }}"
                            data-detail-url="{{ path('app_admin_clinic_show', {id: '0000000000'}) }}">
                            <thead>
                                <tr>
                                    <th>#ID</th>
                                    <th>#ACTIF</th>
                                    <th class="text-uppercase">#Nom - Prénom</th>
                                    <th class="text-uppercase">#Nom de l'établissement</th>
                                    <th>#COURRIEL</th>
                                    <th>#DATE D'INCRIPTION</th>
                                    <th class="text-uppercase">#Spécialité</th>
                                    <th class="text-uppercase">#Abonnement jusqu'au</th>
                                    <th class="text-uppercase">#Inst. Restante</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody></tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block bodyjs %}
{{ parent() }}
<script src="{{ versioned_asset('admin/custom/js/clinic.js') }}"></script>
{% endblock %}