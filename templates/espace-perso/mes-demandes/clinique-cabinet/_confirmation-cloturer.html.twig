<div
    id="modal-confirmation-cloturer"
    class="fixed inset-0 bg-black bg-opacity-50 flex hidden items-center justify-center z-50"
>
    <!-- Modal Content Box -->
    <div class="bg-white p-10 rounded-lg shadow-lg w-1/4">

        <!-- Modal Header -->
        <div class="flex justify-between items-center mb-4">
            <h2 class="text-xl blue-span font-semibold">Confirmation</h2>
            <button class="text-gray-500 hover:text-gray-700 close">&times;</button>
        </div>

        <!-- Modal Body -->
        <div class="mt-4">
            <p>En cliquant sur "<span class="font-semibold">Continuer</span>", votre demande sera archivé.</p>
        </div>

        <div class="mt-4 flex gap-2">
            <button
                class="relative form-submit text-white py-2 px-10 btn-confirm-cloturer"
                data-delete-url="{{ path('api_user_delete', { id: app.user.user.id }) }}"
            >
                <span
                    class="btn-spinner hidden absolute top-3 left-3 h-5 w-5 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                <span>Continuer</span>
            </button>
            <button class="bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded close">
               Annuler
            </button>
        </div>
    </div>
</div>