<div class="bg-blue p-4 flex flex-col gap-10">
	<h1 class="text-xl">
		<b>Validation</b>
	</h1>
	<div class="flex flex-row gap-2">
		<label for="user-cgu-accepted">
			<input type="checkbox" id="user-cgu-accepted" name="cguAccepted" class="form-signup-element" data-checked-value="1" data-unchecked-value="0" class="form-checkbox"/>
		</label>
		<div>
			J'accepte les
			<a href="{{ path('app_node') }}" target="_blank" class="text-blue-500">conditions générales d'utilisation</a>
			et m'engage à les respecter. *
		</div>
	</div>
	<div class="w-full">
		* Données obligatoire
	</div>
	<div class="w-full flex justify-center items-center gap-10">
		<button type="submit" class="relative text-xl form-submit text-white pt-2 pb-2 pr-10 pl-10 form-signup-btn-submit">
			<span class="btn-spinner hidden absolute top-3 left-3 h-5 w-5 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
			<span>Valider</span>
		</button>
		{% if is_granted('ROLE_USER') %}
			<a href="{{ path('app_user_delete') }}" id="btn-se-desinscrire" class="custom-submit text-white p-3 pt-1 pb-1 rounded text-xl">Me désinscrire</a>
		{% endif %}
	</div>
</div>
